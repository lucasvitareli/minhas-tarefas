// defining enums for Priority and Status
// enums are a way to define a set of named constants

export enum Priority {
  URGENT = 'Urgent',
  IMPORTANT = 'Important',
  NORMAL = 'Normal'
}

export enum Status {
  PENDING = 'Pending',
  DONE = 'Done'
}
